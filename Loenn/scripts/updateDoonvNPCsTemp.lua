local entities = require("entities")
local utils = require("utils")

local script = {
    name = "updateDoonvNPCsTemp",
    displayName = "Update Doonv NPCs",
    tooltip = "blah"
}

function script.run(room, args)
    
end

return script